%h1= t("Survey")

%p= t("Welcome text")

%p= t("Instruction")

%table
  %tr
    %th= t("Font name")
    %th= t("Logo")
    %th= t("Votes count")
    %th= t("Vote!")
  - @logos.each do |logo|
    %tr
      %td=h logo.name
      %td= image_tag(logo.url)
      %td= logo.votes_count
      %td ...