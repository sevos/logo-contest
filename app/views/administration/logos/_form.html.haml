- semantic_form_for [:administration, @logo], :html => {:multipart => true} do |f|
  - f.inputs do
    = f.input :name, :label => t("Name")
    = f.input :image, :label => t("Image file")
  - f.buttons do
    = f.commit_button t("Save logo")